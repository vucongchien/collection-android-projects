<!--suppress ALL -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Màu nền -->
    <corners android:radius="25dp" />
    <stroke android:width="0dp" android:color="@android:color/white" /> <!-- Viền -->
</shape>